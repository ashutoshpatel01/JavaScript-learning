const myName = "Ashutosh" // can't change
const rollNo = 23232
let email = "ashutos@googlr.com"
var age = 21 // it has scope problem i.e if  another file with same named variable then taht variables get changed
 idea = "NO" // default take as let

console.log(age)

console.table([myName, rollNo, email, age, idea])